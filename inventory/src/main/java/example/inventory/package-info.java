/**
 * このパッケージの型（{@code 在庫}・{@code 棚卸} など）と behavior {@code 棚卸する} は
 * {@code src/main/souther/inventory.sou} からコンパイル時に {@code SoutherProcessor} が生成する。
 * この {@code package-info} は、生成を走らせるための最小のソース（javac はソースが1つ以上
 * ないとアノテーション処理を起動しない）。
 */
package example.inventory;
