/**
 * The types in this package ({@code Todo}, {@code Filter} and the rest) are generated at compile
 * time by {@code SoutherProcessor} from {@code src/main/souther/todomvc.sou}. This
 * {@code package-info} is the minimal source that makes javac run annotation processing at all.
 */
package example.todomvc;
