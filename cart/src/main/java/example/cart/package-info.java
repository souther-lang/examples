/**
 * このパッケージの型（{@code カート}・{@code 見積} など）と behavior {@code 見積る} は
 * {@code src/main/souther/cart.sou} からコンパイル時に {@code SoutherProcessor} が生成する。
 * この {@code package-info} は、生成を走らせるための最小のソース（javac はソースが1つ以上
 * ないとアノテーション処理を起動しない）。
 */
package example.cart;
