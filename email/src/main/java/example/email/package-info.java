/**
 * このパッケージの型（{@code Email} など）は {@code src/main/mdl/email.mdl} から
 * コンパイル時に {@code SoutherProcessor} が生成する。この {@code package-info} は、生成を
 * 走らせるための最小のソース（javac はソースが1つ以上ないとアノテーション処理を起動しない）。
 */
package example.email;
