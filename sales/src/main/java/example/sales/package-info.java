/**
 * このパッケージの型（{@code 注文}・{@code レポート} など）と behavior {@code 集計する} は
 * {@code src/main/souther/sales.sou} からコンパイル時に {@code SoutherProcessor} が生成する。
 * この {@code package-info} は、生成を走らせるための最小のソース（javac はソースが1つ以上
 * ないとアノテーション処理を起動しない）。
 */
package example.sales;
